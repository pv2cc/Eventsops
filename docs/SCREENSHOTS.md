# Dashboard screenshots for slides

Run the dashboard and capture these views:

1. **screenshot_1_overview.png** — Operations tab, map with risk + pre-stage layers, top metrics
2. **screenshot_2_dispatch.png** — After *Run Smart Dispatch*: load gauge, closure badge, before/after
3. **screenshot_3_learning.png** — Learning & patterns tab: retrain history + recurring patterns table

```bash
python -m streamlit run app.py
```

Paste screenshots into your pitch deck under "Demo" and "Impact".
